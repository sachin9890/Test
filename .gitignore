node_modules/
.env
*.log

# Runtime data: cloned projects, their per-session worktrees, and persisted
# project metadata (including PATs). None of this is part of this app's source.
/projects/
/worktrees/
/data/
